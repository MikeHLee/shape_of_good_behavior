# Fine-tuning dependencies for modal_finetune.py
# Uses latest stable TRL (1.x) + transformers for SFT and RM training.
# PPO is implemented manually — no dependency on TRL's PPOTrainer
# (removed in TRL 1.0 anyway). Target: Qwen2.5-1.5B-Instruct on L4.

# Core scientific stack
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

# Embedding & NLP
sentence-transformers>=2.2.0
transformers>=4.40.0
datasets>=2.0.0
tokenizers>=0.15.0

# Fine-tuning stack — pin to TRL 0.11.x for stable pre-tokenized RewardTrainer
# API (chosen/rejected are pre-tokenized into input_ids_chosen / _rejected).
# TRL >=0.13 changed the schema and breaks our HodgeRewardTrainer subclass.
trl>=0.11.0,<0.13.0
peft>=0.10.0
accelerate>=0.28.0
bitsandbytes>=0.43.0

# Visualization
plotly>=5.10.0
matplotlib>=3.7.0

# Utilities
pandas>=2.0.0
tqdm>=4.65.0
pyyaml>=6.0
